rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow authenticated users to read/write all paths (simplified for debugging)
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
